// You have two strings s1 and s2. Write  function that returns true if they both are equal or false if none. Any hash in the string is considered as a backspace. 
// For example:
// s1 = 'ab#c' and s2 = 'az#c' are equal and function will return true;
// s1 = 'abbc' and s2 = 'abdc' are not and function will return false; 

const s1 = 'ab#c';
const s2 = 'az#c';

const backSpaceCompare = (S1, S2) => {
  // write your solution here
}