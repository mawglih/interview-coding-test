import axios from 'axios';
import { put } from 'redux-saga/effects';
import * as actions from 'actions';

const fetchURL = 'http://localhost:3001/get-items';

export function* fetchProductsSaga() {
  // finish saga 
};