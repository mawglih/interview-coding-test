// By given array of integers write a function that finds indeces of two numbers in the array that sum to the given number. Return indeces, or -1 if none found.

const numbers = [1,4,7,3,10,13];

const findSum = (arr, target) => {
  // write the function
};

